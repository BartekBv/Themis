#include<bits/stdc++.h>
using namespace std;


int a;
int tr[4];
int main(){
	cin>>a;
	while(a--){
		for(int i=0;i<3;i++){
			cin>>tr[i];
		}
		sort(tr, tr+3);
		if((tr[0]*tr[0])+(tr[1]*tr[1])==(tr[2]*tr[2])) cout<<"TAK\n";
		else cout<<"NIE\n";
		
	}
}